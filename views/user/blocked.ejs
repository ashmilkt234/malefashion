<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Account Temporarily Restricted</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
<link rel="shortcut icon" href="/img/hero/cr.jpg" type="image/png">
  <style>
    body {
      margin: 0;
      font-family: 'Poppins', sans-serif;
      background: radial-gradient(circle at top, #1f2933, #0f172a);
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #fff;
    }

    .blocked-card {
      max-width: 460px;
      width: 90%;
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(18px);
      border-radius: 18px;
      padding: 40px 35px;
      text-align: center;
      box-shadow: 0 25px 60px rgba(0, 0, 0, 0.45);
      animation: fadeIn 0.7s ease-in-out;
    }

    .icon {
      width: 90px;
      height: 90px;
      border-radius: 50%;
      background: linear-gradient(135deg, #ef4444, #dc2626);
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 25px;
      box-shadow: 0 10px 30px rgba(239, 68, 68, 0.6);
    }

    .icon span {
      font-size: 42px;
    }

    h1 {
      font-size: 22px;
      font-weight: 600;
      margin-bottom: 12px;
    }

    p {
      font-size: 14.5px;
      line-height: 1.6;
      color: #cbd5e1;
      margin-bottom: 28px;
    }

    .info-box {
      background: rgba(255, 255, 255, 0.08);
      border-left: 4px solid #ef4444;
      padding: 14px 16px;
      font-size: 13px;
      border-radius: 8px;
      color: #e5e7eb;
      margin-bottom: 30px;
    }

    .actions {
      display: flex;
      gap: 14px;
      justify-content: center;
      flex-wrap: wrap;
    }

    .btn {
      padding: 12px 22px;
      border-radius: 30px;
      font-size: 13.5px;
      text-decoration: none;
      font-weight: 500;
      transition: all 0.25s ease;
    }

    .btn-home {
      background: #2563eb;
      color: #fff;
    }

    .btn-home:hover {
      background: #1d4ed8;
    }

    .btn-support {
      border: 1px solid #475569;
      color: #e5e7eb;
    }

    .btn-support:hover {
      background: rgba(255, 255, 255, 0.08);
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(10px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>
<body>

  <div class="blocked-card">
    <div class="icon">
      <span>⛔</span>
    </div>

    <h1>Account Temporarily Restricted By Admin</h1>
    <p>
      We’ve detected an issue with your account.  
      For security and policy reasons, access has been temporarily limited.
    </p>

    <div class="info-box">
      If you believe this is a mistake, please contact our support team for further assistance.
    </div>

    <div class="actions">
      <a href="/" class="btn btn-home">Go to Homepage</a>
      <a href="/contact" class="btn btn-support">Contact Support</a>
    </div>
  </div>

</body>
</html>
